{
  "name": "project-gen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check src/authority.mjs && node --check src/cache.mjs && node --check src/task-graph.mjs && node --check src/budget.mjs",
    "test": "node --test"
  }
}
